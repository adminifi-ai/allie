id: axe-target-size-pass-flow
name: Target-size pass fixture flow
app_name: Allie fixture app
environment: local-fixture
auth_profile: local-fixture-no-auth
credentials:
  profile: local-fixture-no-auth
  provider: none
  required: false
target:
  kind: local_fixture
  fixture_dir: ../fixtures/axe-target-size-pass
policy:
  profile: wcag22-aa
  blocking_classes:
    - deterministic
    - scripted_required
  worker_timeout_ms: 30000
artifacts:
  redaction_status: not_redacted_local_fixture
  retention_class: local_ephemeral
model:
  enabled: false
  provider_allowlist: []
  zdr_required: true
known_nondeterminism: []
browser:
  viewport:
    width: 1280
    height: 900
  color_scheme: light
  reduced_motion: reduce
  locale: en-US
  zoom: 1.0
flow:
  id: axe-target-size-pass-critical-path
  description: >-
    Inspect a fixture with an interactive target large enough for axe-core
    target-size to evaluate and pass.
  states:
    - id: target-size-pass
      path: /
      description: Fixture route with an evaluated passing target-size control.
      required: true
      axe: true
      screenshot: true
